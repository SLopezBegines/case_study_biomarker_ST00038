# config/

Configuration files and parameter sets. Centralizing parameters here makes
analyses easier to reproduce and adjust without touching script logic.

Example files: `params.yaml`, `sample_sheet.csv`, `paths.yaml`.

# code/

Reusable scripts, functions, and modules. Prefer small, single-purpose files.

- `utils/` — helper functions shared across scripts/notebooks.
- Name scripts with a numeric prefix to indicate execution order (e.g. `01_preprocess.py`).
